@AGENTS.md

# Claude Instructions

The root `AGENTS.md` policy is imported above and is the canonical repository
guide.

Before making changes:

1. Read the closest scoped `AGENTS.md` for the paths you will edit.
2. Check `docs/agents/module-map.yaml` for ownership and validation commands.
3. Use repo-scoped skills only for matching task-specific workflows.

Do not duplicate or override repository policy here. If instructions need to
change, update `AGENTS.md` and the relevant scoped guide.
